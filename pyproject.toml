[project]
name = "ecom-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "javicupeiro", email = "javier.cupeiro.martin@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.105.2",
    "fastapi>=0.136.3",
    "openai>=2.41.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "uvicorn>=0.49.0",
]

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
